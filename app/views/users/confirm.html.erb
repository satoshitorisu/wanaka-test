<%= render 'partial/layout/navbar'%>
<div class="content">
	<h1>入力情報</h1>
	<hr>
	<br>
    <div class="form-group">
		<h2>ユーザーネーム：<%= @user.username %></h2>
	</div>
    <div class="form-group">
		<h2>お名前：<%= @user.first_name %> <%= @user.last_name %></h2>
	</div>

    <div class="form-group">
    	<h2>
		生まれ年：<%= @user.birth_day.strftime("%Y年") %><br>
    	</h2>
	</div>
    <div class="form-group">
    	<h2>性別：
	    	<% if  @user.sex == "male" %>
	    		男性
			<% else %>
				女性
	    	<% end %>
    	</h2>
	</div>
	<hr>
	<small>入力情報をご確認ください。<br>よろしければ登録ボタンをクリックしてください。</small>
    <div class="form-group">
		<%= form_for(:user, url: user_complete_path, method: :post ) do |f|  %>
		    <%= f.hidden_field :username %>
		    <%= f.hidden_field :first_name %>
		    <%= f.hidden_field :last_name %>
		    <%= f.hidden_field :first_name_kana %>
		    <%= f.hidden_field :last_name_kana %>
		    <%= f.hidden_field :birth_day %>
		    <%#= f.hidden_field :image %>
		    <%= f.hidden_field :sex %>
		    <%= f.submit '登録' , class:"wanaka-btn btn-width-wide btn-complete" %> 
		<% end %>
	</div>
</div>

<%= render 'partial/layout/footer'%>